package lesson.selection.tasks;

public class Tasks {
    // --------------------
    // Section 1
    // --------------------

    // Task 1
    // Complete the function isEven(x) that returns true if a given integer x
    // is even.
    boolean isEven(int x) {
        return false; // TODO Remove this line
    }

    // Task 2
    // Write a function abs(x) that takes a double x as input and returns the
    // magnitude of the value.
    //
    // e.g.
    // - if x is 5, the result is positive 5
    // - if x is -3.14, the result is positive 3.14
    //
    // Note: This function is already provided in Java as the Math.abs function,
    // but do not use that in your solution.

    // TODO Write the abs(x) function here

    // --------------------
    // Section 2
    // --------------------

    // Task 1
    // Write a function largestOf(a, b, c) that takes 3 doubles as input and
    // returns the largest value.

    // TODO Write the largestOf(a, b, c) function here

    // Task 2
    // Write a function order(a, b, c) that takes 3 doubles as input and
    // returns a 3-digit number where each digit represents one of the inputs
    // and the value is 1 if it is the largest, 2 if it is the middle, and 3 if
    // it is the smallest.
    //
    // e.g.
    // - order(46, 1, 3) returns 132
    // - order(5, 9, -3) returns 213
    //
    // Challenge: If there is a tie, make the values the same
    // - order(58, 0, 0) returns 122
    // - order(1, 1, 1) returns 111
    // - order(5, 5, 3) returns 113

    // TODO Write the order(a, b, c) function here
}